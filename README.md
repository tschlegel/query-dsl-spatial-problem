# Working example spring data jpa with query dsl and spatial support 

This minimal example for spring data jpa with query dsl and spatial support. Code is generated by the maven-apt-plugin.

Lombok added to reduce boilerplate code.
